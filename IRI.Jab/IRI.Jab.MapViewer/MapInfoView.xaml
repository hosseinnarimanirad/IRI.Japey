<UserControl x:Class="IRI.Jab.MapViewer.MapInfoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IRI.Jab.MapViewer">
    <UserControl.Resources>
        <Style  TargetType="{x:Type Button}" x:Key="circleButton">
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Background" Value="{DynamicResource AccentColorBrush3}"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid>
                            <Ellipse Margin="1" Fill="{TemplateBinding Property=Background}"/>
                            <ContentPresenter/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentColorBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Border CornerRadius="15" BorderThickness="2" MinWidth="200" VerticalAlignment="Top" HorizontalAlignment="Center" 
                    Background="{DynamicResource AccentColorBrush3}" BorderBrush="#77FFFFFF"  
                    Margin="4" Visibility="{Binding IsNewDrawOrEditMode, Converter={StaticResource boolToVisivilityConverter}}">
        <DockPanel Margin="0">
            <TextBlock Text="برای اتمام ترسیم روی نقطهٔ آخر مجدد کلیک کنید." VerticalAlignment="Center" Margin="8,0" Foreground="White"/>
            <Button Style="{StaticResource circleButton}" DockPanel.Dock="Left" HorizontalAlignment="Right" Margin="2" 
                            Command="{Binding CancelNewDrawingCommand}" Visibility="{Binding IsDrawMode, Converter={StaticResource boolToVisivilityConverter}}">
                <Path Data="{StaticResource appbarClose}" Stretch="Uniform" Margin="8" Fill="White" FlowDirection="LeftToRight"/>
            </Button>
            <Button Style="{StaticResource circleButton}" DockPanel.Dock="Left" HorizontalAlignment="Right" Margin="2" 
                            Command="{Binding CancelEditDrawingCommand}" Visibility="{Binding IsEditMode, Converter={StaticResource boolToVisivilityConverter}}">
                <Path Data="{StaticResource appbarClose}" Stretch="Uniform" Margin="8" Fill="White" FlowDirection="LeftToRight"/>
            </Button>
            <Button Style="{StaticResource circleButton}" DockPanel.Dock="Left" HorizontalAlignment="Right" Margin="2" 
                            Command="{Binding FinishEditDrawingCommand}" Visibility="{Binding IsEditMode, Converter={StaticResource boolToVisivilityConverter}}">
                <Path Data="{StaticResource appbarCheck}" Stretch="Uniform" Margin="8" Fill="White" FlowDirection="LeftToRight"/>
            </Button>
        </DockPanel>
    </Border>
</UserControl>
