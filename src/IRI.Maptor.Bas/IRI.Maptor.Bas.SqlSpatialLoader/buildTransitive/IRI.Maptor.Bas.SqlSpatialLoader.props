<Project>
  <PropertyGroup>
    <!-- Define a constant for consumers -->
    <DefineConstants>$(DefineConstants);MAPTOR_SQLSPATIAL_LOADER</DefineConstants>
  </PropertyGroup>

  <Target Name="MaptorSqlSpatialLoader_Hint" BeforeTargets="Build">
    <Message Importance="Low" Text="Maptor: SqlSpatialLoader included. Call SqlSpatialLoader.Load() at startup if SQL types are used without native installation." />
  </Target>
</Project>
